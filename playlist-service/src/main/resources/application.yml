spring:
  application:
    name: playlist-service

  datasource:
    url: jdbc:postgresql://localhost:5433/playlists_db
    username: postgres
    password: 123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 9004

eureka:
  instance:
    hostname: localhost

  client:
    service-url:
      defaultZone: http://localhost:8761/eureka