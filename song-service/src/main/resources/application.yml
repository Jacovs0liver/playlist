spring:
  application:
    name: song-service

  datasource:
    url: jdbc:postgresql://localhost:5433/songs_db
    username: postgres
    password: 123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 9003

eureka:
  instance:
    hostname: localhost

  client:
    service-url:
      defaultZone: http://localhost:8761/eureka