spring:
  application:
    name: artist-service

  datasource:
    url: jdbc:postgresql://localhost:5433/artists_db
    username: postgres
    password: 123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 9002

eureka:
  instance:
    hostname: localhost

  client:
    service-url:
      defaultZone: http://localhost:8761/eureka